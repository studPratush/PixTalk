<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.asd">
    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".QnALevel1"></activity>
        <activity android:name=".QnAMenu" />
        <activity android:name=".Counting" />
        <activity android:name=".SWLAudio" />
        <activity android:name=".SWLImages" />
        <activity android:name=".SWL" />
        <activity android:name=".Differentiate" />
        <activity android:name=".ChildMenu" />
        <activity android:name=".ImageGeneration" />
        <activity
            android:name=".Signup"
            android:label="@string/title_activity_signup" />
        <activity android:name=".Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Login"
            android:label="@string/title_activity_login"
            android:theme="@style/AppTheme" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>